## Packages
framer-motion | Page transitions and sleek animated interactions
date-fns | Human-readable date formatting for race weekends

## Notes
F1 styling uses carbon fiber textures and primary racing red.
Backend enforces max 10 users constraint on registration, handled via 409 conflict.
Auth handles /api/me to return current user or 401.
